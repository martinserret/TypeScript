// .d.ts files are TypeScript declaration files (or type declaration files). They don't specify code that should be executed. 
// They contain just type definitions that can be used by other files, but that can't be executed (doesn't compile to JavaScript).


// Here, points at some other file that is part of the vite package which is stored in the node_modules folder.
// Contains type definitions for Vite's environment variables and other global types that Vite provides.
/// <reference types="vite/client" /> 

